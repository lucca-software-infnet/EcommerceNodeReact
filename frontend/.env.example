#####################################################################
# Frontend - exemplo de variáveis de ambiente (Vite)
#
# Por padrão, o projeto usa o proxy do Vite (/api -> http://localhost:3333)
# então você pode não precisar definir nada.
#####################################################################

# Opcional:
# Se você quiser apontar direto para o backend (sem proxy),
# defina a URL BASE do backend (o código adiciona /api automaticamente se faltar).
#
Exemplos válidos:
 VITE_API_URL=http://localhost:3333
 VITE_API_URL=http://localhost:3333/api
 VITE_API_URL=/api

