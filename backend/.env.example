#####################################################################
# Backend - exemplo de variáveis de ambiente (desenvolvimento)
#
# IMPORTANTE:
# - Não commite um `.env` com secrets reais.
# - O docker-compose usa `backend/.env` como env_file. Para rodar via Docker:
#   copie este arquivo para `backend/.env` e ajuste conforme necessário.
#####################################################################

# Porta do Fastify (docker-compose expõe 3333)
PORT=3333

# Prisma (MySQL no docker-compose)
# Formato: mysql://USER:PASS@HOST:PORT/DB
DATABASE_URL=mysql://root:root@mysql:3306/ecommerceNode

# Redis (docker-compose: service "redis")
REDIS_URL=redis://redis:6379

# URL do frontend (usada para CORS e links de email)
FRONTEND_URL=http://localhost:5173


# Mercado Pago (Checkout Pro)
# Gere o Access Token no painel do Mercado Pago (credenciais de produção ou teste).
MERCADO_PAGO_ACCESS_TOKEN=APP_USR-8762470911224493-020920-637e76319d1a3089ed323cde72067dde-3193873308

# Cookies (em HTTPS cross-site use COOKIE_SECURE=true)
COOKIE_SECURE=false

# JWT (use valores fortes em produção)
JWT_ACCESS_SECRET=dev_jwt_access_secret
JWT_REFRESH_SECRET=dev_jwt_refresh_secret
JWT_ISSUER=ecommerce-api

# Email (opcional: se não configurar, o projeto loga no console em dev)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=you@example.com
# EMAIL_PASS=your_password

